import 'container.dart';
import 'exception_handler.dart';

class AppExceptionHandler extends ExceptionHandler {
  AppExceptionHandler(Container container) : super(container);

  @override
  void register() {
    //
  }

}
